DEBUG=True
SECRET_KEY=change-me-in-production
ALLOWED_HOSTS=localhost,127.0.0.1

# Leave DATABASE_URL unset to use local SQLite (zero setup, good for dev).
# Example for Postgres in production:
# DATABASE_URL=postgres://user:password@localhost:5432/restaurant_saas

# Leave REDIS_URL unset to use Channels' in-memory layer (single-process dev).
# Example for Redis in production:
# REDIS_URL=redis://localhost:6379/0

CORS_ALLOWED_ORIGINS=http://localhost:5173
FRONTEND_BASE_URL=http://localhost:5173
